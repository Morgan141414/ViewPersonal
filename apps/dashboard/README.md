# Dashboard (React + Vite)

## Local run

1) Install deps:
- `cd apps/dashboard`
- `npm install`

2) Start dev server:
- `npm run dev`

Open http://localhost:5173

By default the dashboard calls API at `http://localhost:8000`.
