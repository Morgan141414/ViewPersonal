services:
  api:
    build:
      context: ../services/api
    env_file:
      - ../services/api/.env.example
    ports:
      - "8000:8000"

  dashboard:
    build:
      context: ../apps/dashboard
    ports:
      - "5173:5173"
      - "5174:5173"
    depends_on:
      - api
