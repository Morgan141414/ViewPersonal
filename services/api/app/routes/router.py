"""Deprecated FastAPI router.

The project switched to Starlette-only runtime (see `app.main`).
This module is kept as a placeholder to avoid breaking imports if someone still references it.
"""

api_router = None
